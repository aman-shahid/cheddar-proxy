# Scripts

- `benchmark/` – benchmark runners (throughput, latency, startup, memory, process metrics, and `run_process_benchmark.*` wrappers).
- `icons/` – icon extraction utility (`extract_cheese_icon.py`). Backups live under `ui/assets/icon/icon_backup/`.

Most entry points now live here (e.g., `run.sh`, `build_rust.sh`, release/test runners).
